<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE project>

<configuration status="off">
    <appenders>
        <Console name="console-appender" target="SYSTEM_OUT">
            <PatternLayout pattern="[%d{yyyy-MM-dd HH:mm:ss.SSS ${env:HOSTNAME} %-5level [%t] %logger{0} - %msg%n"/>
        </Console>
    </appenders>
    <loggers>
        <Root level="info">
            <appender-ref ref="console-appender"/>
        </Root>
    </loggers>
</configuration>
